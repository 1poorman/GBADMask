# GBADMask 依赖清单
#
# 以下版本组合已在本机实测通过（RTX 3090 / Driver 580 / CUDA 13.0 + nvcc 11.7）。
# detectron2 0.6 与 adet 0.2.0 都是 2022 年的代码，对新版本依赖非常敏感，
# 建议严格按此清单安装。关键约束见下方注释。
#
# 安装顺序：先装本文件，再按 README 5.2 节源码编译 detectron2 与 adet。
#
# ---------------------------------------------------------------------------
# 核心：PyTorch
# ---------------------------------------------------------------------------
# CUDA runtime 11.7，必须与编译 detectron2/adet 所用的 nvcc 大版本一致。
# 若改用 cu118，请把下面三行换成 download.pytorch.org/whl/cu118 的对应版本，
# 并将 CUDA_HOME 指向 /usr/local/cuda-11.8。
torch==2.0.1
torchvision==0.15.2
numpy==1.23.5

# ---------------------------------------------------------------------------
# detectron2 v0.6 依赖
# ---------------------------------------------------------------------------
fvcore==0.1.5.post20221221
iopath==0.1.10
yacs==0.1.8
pycocotools==2.0.6
# ⚠️ detectron2 v0.6 使用 PIL.Image.LINEAR，Pillow 10+ 已移除该常量，必须 <10
Pillow==9.5.0
omegaconf==2.3.1
matplotlib==3.9.4
scipy==1.13.1
shapely==2.0.7
termcolor==3.1.0
cloudpickle==3.1.2
tabulate==0.9.0
tensorboard==2.21.0
tqdm==4.70.0

# ---------------------------------------------------------------------------
# Vision GNN 骨干（cspvig / Lcspvig）
# ---------------------------------------------------------------------------
# ⚠️ 必须 <1.0：timm 1.0 起移除了 timm.models.layers.ConvBnAct 等 API
timm==0.6.12

# ---------------------------------------------------------------------------
# adet 其他依赖
# ---------------------------------------------------------------------------
# adet/evaluation/text_eval_script.py 依赖；tools/train_*.py 会 import 它
rapidfuzz
# BAText 语法解析
antlr4-python3-runtime==4.8
ply==3.11

# ---------------------------------------------------------------------------
# 可选：onnx/ 目录下的导出与推理脚本
# ---------------------------------------------------------------------------
# onnx
# onnxruntime
